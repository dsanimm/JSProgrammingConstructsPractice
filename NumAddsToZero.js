const prompt = require('prompt-sync')();
let numbers = new Array();
numb[0] = prompt('Enter first num : ');
numb[1] = prompt('Enter second num : ');
numb[2] = parseInt(0 - (parseInt(numb[0]) + parseInt(num[1])));
console.log(numbers);
let sum = 0;
numbers.forEach(num => { sum += parseInt(num); });
console.log('Sum is : ' + sum);